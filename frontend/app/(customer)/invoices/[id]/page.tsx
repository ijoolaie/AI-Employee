"use client";
import { useState } from "react"; import { useParams } from "next/navigation"; import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query"; import { Header } from "@/components/layout/header"; import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Spinner } from "@/components/ui/spinner"; import { getErrorMessage,getInvoice,updateInvoiceStatus,exportInvoicePdf } from "@/lib/api";
export default function InvoiceDetail(){const {id}=useParams<{id:string}>();const qc=useQueryClient();const [msg,setMsg]=useState("");const q=useQuery({queryKey:["invoice",id],queryFn:()=>getInvoice(id)});const status=useMutation({mutationFn:(s:string)=>updateInvoiceStatus(id,s),onSuccess:()=>{setMsg("Status updated");qc.invalidateQueries({queryKey:["invoice",id]});qc.invalidateQueries({queryKey:["invoices"]})}});const pdf=useMutation({mutationFn:()=>exportInvoicePdf(id),onSuccess:d=>setMsg(d?.url?`PDF ready: ${d.url}`:"PDF export completed")});if(q.isLoading)return <Spinner/>;if(q.isError)return <p className="p-6 text-red-600">{getErrorMessage(q.error)}</p>;const i=q.data!;return <><Header title={`Invoice ${i.number}`} description={`${i.customer_name} · ${i.status}`}/><div className="space-y-6 p-6"><Card><CardContent className="grid gap-4 p-6 md:grid-cols-4 text-sm"><div><span className="text-gray-500">Customer</span><p className="font-semibold">{i.customer_name}</p></div><div><span className="text-gray-500">Issue date</span><p>{i.issue_date}</p></div><div><span className="text-gray-500">Due date</span><p>{i.due_date??"—"}</p></div><div><span className="text-gray-500">Total</span><p className="font-semibold">{Number(i.total).toLocaleString()} {i.currency}</p></div></CardContent></Card><Card><CardHeader><CardTitle>Line items</CardTitle></CardHeader><CardContent><div className="space-y-2">{(i.line_items??[]).map((x:any,n:number)=><div key={n} className="flex justify-between border-b py-2 text-sm"><span>{x.description} × {x.quantity}</span><span>{Number(x.amount??x.unit_price*x.quantity).toLocaleString()} {i.currency}</span></div>)}</div><div className="mt-4 text-right font-semibold">Total: {Number(i.total).toLocaleString()} {i.currency}</div></CardContent></Card><div className="flex flex-wrap gap-2"><select className="rounded-lg border px-3 py-2 text-sm" value={i.status} onChange={e=>status.mutate(e.target.value)}>{["draft","sent","paid","overdue","void"].map(s=><option key={s}>{s}</option>)}</select><Button variant="secondary" onClick={()=>pdf.mutate()} disabled={pdf.isPending}>{pdf.isPending?"Exporting…":"Export PDF"}</Button></div>{msg&&<p className="text-sm text-gray-600">{msg}</p>}</div></>}
