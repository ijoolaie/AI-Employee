# Architecture checks

Run from the project root:

    python scripts/check_architecture.py

The same check runs automatically on pull requests and pushes through
`.github/workflows/architecture.yml`.
