# v1.4.8 WhatsApp Idempotency Release Audit

**Date:** 2026-09-20  
**Repository:** `ijoolaie/AI-Employee`  
**Target release:** `v1.4.8`  
**Status:** MERGED TO MAIN / NOT YET TAGGED OR CERTIFIED

## Exact engineering boundary

- PR: #550
- Feature branch head before merge: `1bc4bed7592d8f6ba00bb832b19db8707bc519e6`
- Merge commit: `ea37b75d599761b91fed33e26ff13e552ae2b963`
- PR: #551 — real PostgreSQL WhatsApp concurrency coverage
- PR #551 merge commit: `542ed5f161d306ed2a3993995562b108961dc270`
- PR #551 validated head: `b96ed2477e48dcad29ea99fd9581c064048b79df`
- v1.4.7 certified SHA remains immutable: `48a6df0ea8a2fb0624e831fbdea55ee4548807f6`

No certification is transferred from v1.4.7 to v1.4.8.

## Scope verified in source

### WhatsApp provider-message idempotency

- `CustomerMessage.provider_message_id` persists the provider message identifier.
- Uniqueness is scoped to `tenant_id + channel_id + provider_message_id`.
- The unique index is partial and applies only when the provider identifier is non-null.
- Duplicate admission returns the existing conversation/run and does not create another Run.

### WhatsApp conversation admission

- WhatsApp conversations use `external_conversation_key = wa:<phone>`.
- Uniqueness is scoped to `channel_id + external_conversation_key`.
- PostgreSQL uniqueness is the final concurrency guard.
- Conversation creation handles `IntegrityError` inside a SAVEPOINT and then reads the winning row.
- Legacy conversations without the new key have a compatibility path using row locking.

### Transactional execution hand-off

- WhatsApp no longer calls Celery directly from the API transaction.
- The handler persists an `agent.run.execute` transactional-outbox row with the Run and message.
- The outbox dispatcher queues Celery only after the database transaction has committed.
- The outbox row has a deterministic WhatsApp dedupe key.

This closes the previously identified API-commit-versus-worker-start race.

## CI evidence

The final PR #550 head `1bc4bed7592d8f6ba00bb832b19db8707bc519e6` passed all observed pull-request workflows. PR #551 then added real PostgreSQL concurrency coverage; its final head `b96ed2477e48dcad29ea99fd9581c064048b79df` passed the full CI and supporting gates:

- CI — PASS
- Architecture Guard — PASS
- CodeQL — PASS
- Runtime Isolation and RBAC Contract — PASS
- HA Failure Recovery Validation — PASS
- Ephemeral DAST Validation — PASS
- Production Infrastructure Validation — PASS
- Production Observability — PASS
- Production Rollback & Alerting — PASS

PR #550 was merged as `ea37b75d599761b91fed33e26ff13e552ae2b963`; PR #551 was merged as `542ed5f161d306ed2a3993995562b108961dc270`.

## Regression coverage

PR #551 provides the real PostgreSQL race evidence described below. The focused test contract covers:

- duplicate provider message returns the existing Run;
- provider ID fields exist on the message model;
- WhatsApp dispatch uses the transactional outbox rather than direct Celery dispatch.

The CI backend suite and migration graph checks passed. For PR #551 head `b96ed2477e48dcad29ea99fd9581c064048b79df`, CI run `35516341466` (#1579) passed, and Architecture Guard, CodeQL, Runtime Isolation/RBAC, HA, DAST, Infrastructure, Observability and Rollback/Alerting gates also passed.

## PostgreSQL race test implementation

Added `backend/tests/test_whatsapp_webhook_postgres_race.py` with real `AsyncSessionLocal` PostgreSQL concurrency coverage:

- concurrent identical provider message IDs → one conversation, one message, one Run admission;
- concurrent same sender with different provider IDs → one conversation, two messages, two independent Run admissions.

The tests exercise the actual PostgreSQL partial unique indexes and the SAVEPOINT/`IntegrityError` recovery paths in `_enqueue_whatsapp_message`; only Run creation and outbox dispatch are stubbed so the test remains focused on database admission rather than AI/Celery execution.

The CI environment already provisions PostgreSQL 16 and runs the complete backend pytest suite, so this test is included automatically in the backend test job.

## Remaining v1.4.8 validation boundary

The following PostgreSQL race items are now **completed** on PR #551 and passed the full CI backend suite:

1. Real PostgreSQL concurrent same-provider-ID webhook admission — PASS.
2. Real PostgreSQL concurrent same-sender/different-provider-ID admission — PASS.
3. Same sender with different provider IDs produces two messages/runs in one conversation — PASS.

The remaining items are:

4. Public Chat regression confirms multiple conversations per customer remain possible.
5. Meta webhook replay runtime against the actual API stack.
6. Exact-SHA Production Certification for v1.4.8.

## Release truth

At this point:

- `v1.4.7` = published, exact-SHA certified, immutable.
- `main` = contains the merged v1.4.8 WhatsApp hardening plus real PostgreSQL race coverage (merge `542ed5f161d306ed2a3993995562b108961dc270`).
- `v1.4.8` = engineering candidate; **not yet tagged, certified, externally deployed, or externally accepted**.

