# v1.4.8 WhatsApp Idempotency Release Audit

**Date:** 2026-09-21  
**Repository:** `ijoolaie/AI-Employee`  
**Target release:** `v1.4.8`  
**Status:** PUBLISHED / EXACT-SHA CERTIFIED / IMMUTABLE RELEASE

## Exact engineering boundary

- PR #550 — WhatsApp webhook idempotency and conversation race hardening — merge `ea37b75d599761b91fed33e26ff13e552ae2b963`.
- PR #551 — real PostgreSQL WhatsApp concurrency coverage — merge `542ed5f161d306ed2a3993995562b108961dc270`.
- PR #552 — Public Chat multiple-conversation regression coverage — merge `84b0e9d50f25095e5a2e38d051754ed077509f4d`.
- PR #553 — Meta WhatsApp webhook replay idempotency runtime coverage.
- PR #553 validated head: `2ad5aa3ceb1f5626d1a2edebd8705679ecfea440`.
- PR #553 merge commit / current mainline head: `4f7c4676850b546a1c6bdf219ab9401202302e2d`.
- v1.4.7 certified SHA remains immutable: `48a6df0ea8a2fb0624e831fbdea55ee4548807f6`.
- v1.4.8 exact certification target: `4f7c4676850b546a1c6bdf219ab9401202302e2d`.

No certification is transferred from v1.4.7 to v1.4.8.

## Scope verified in source

### WhatsApp provider-message idempotency

- `CustomerMessage.provider_message_id` persists the provider message identifier.
- Uniqueness is scoped to `tenant_id + channel_id + provider_message_id`.
- The unique index is partial and applies only when the provider identifier is non-null.
- Duplicate admission returns the existing conversation/run and does not create another Run.

### WhatsApp conversation admission

- WhatsApp conversations use `external_conversation_key = wa:<phone>`.
- Uniqueness is scoped to `channel_id + external_conversation_key`.
- PostgreSQL uniqueness is the final concurrency guard.
- Conversation creation handles `IntegrityError` inside a SAVEPOINT and then reads the winning row.
- Legacy conversations without the new key have a compatibility path using row locking.

### Transactional execution hand-off

- WhatsApp no longer calls Celery directly from the API transaction.
- The handler persists an `agent.run.execute` transactional-outbox row with the Run and message.
- The outbox dispatcher queues Celery only after the database transaction has committed.
- The outbox row has a deterministic WhatsApp dedupe key.

This closes the previously identified API-commit-versus-worker-start race.

## PR #553 Meta replay/runtime evidence

PR #553 was merged after the final head `2ad5aa3ceb1f5626d1a2edebd8705679ecfea440` passed all six observed workflows:

- CI #1585 — PASS
- Architecture Guard #1588 — PASS
- CodeQL #1970 — PASS
- Production Infrastructure Validation #848 — PASS
- HA Failure Recovery Validation #591 — PASS
- Ephemeral DAST Validation #804 — PASS

The focused test no longer depends on the FastAPI TestClient/OpenTelemetry instrumentation path. It calls the actual `whatsapp_meta_inbound` handler, constructs the raw Meta payload, computes the real `sha256=` HMAC using the configured Meta app secret, sends the signed request twice in separate database sessions, and verifies:

- first delivery: `processed=1, duplicates=0`;
- replay: `processed=0, duplicates=1`;
- exactly one persisted `CustomerMessage`;
- exactly one persisted `Run`;
- the message remains linked to the admitted Run.

This is runtime-handler and database evidence in CI, not evidence of a live Meta/production endpoint.

## Regression coverage

PR #551 provides the real PostgreSQL race evidence:

- duplicate provider message returns the existing Run;
- provider ID fields exist on the message model;
- WhatsApp dispatch uses the transactional outbox rather than direct Celery dispatch;
- concurrent identical provider IDs converge to one message/Run;
- concurrent same sender with different provider IDs remain two messages/runs in one conversation.

PR #552 adds real PostgreSQL Public Chat coverage proving:

- the same customer can intentionally have multiple independent conversations;
- concurrent starts for the same customer create independent conversations/tokens;
- the v1.4.8 WhatsApp external conversation key does not impose a global `channel_id + customer_id` uniqueness constraint.

## v1.4.8 validation boundary

The following items are completed:

1. Real PostgreSQL concurrent same-provider-ID webhook admission — PASS.
2. Real PostgreSQL concurrent same-sender/different-provider-ID admission — PASS.
3. Same sender with different provider IDs produces two messages/runs in one conversation — PASS.
4. Public Chat regression confirms multiple independent conversations per customer, including concurrent starts — PASS.
5. Signed Meta webhook replay/idempotency handler verification against real PostgreSQL — PASS.

The implementation and exact-SHA certification boundary is complete. The `v1.4.8` release is now tagged and published at the certified SHA `4f7c467...`.

## Release truth

At this point:

- `v1.4.8` = published, exact-SHA certified, immutable at `4f7c4676850b546a1c6bdf219ab9401202302e2d`.
- Current `main` = `b0b5c8492626901a72303ae0fe1bd99eeef6429d`, 19 commits newer and not certified.
- The post-release delta is documentation reconciliation plus frontend dependency updates.
- Certification does not transfer to current main.
- Next release boundary: fresh exact-SHA certification of the eventual `v1.4.9` candidate.
