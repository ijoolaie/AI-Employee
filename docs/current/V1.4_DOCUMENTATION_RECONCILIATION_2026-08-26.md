# V1.4 Documentation Reconciliation — 2026-08-26

## Purpose

Correct the repository documentation so the **current implementation baseline** is not confused with the **latest certified vendor release**.

## Correct version model

- **Current repository implementation baseline:** **V1.4 ACTIVE EXECUTION BASELINE**.
- **Latest certified controlled-deployment vendor release:** **v1.2.0**.
- V1.4 is an active architecture/gap-closure track and is already partially implemented; it is not merely a future plan.
- v1.2.0 remains the certified vendor release identity and must not be relabeled as the current V1.4 implementation state.

## Evidence

The repository's V1.4 execution status records the completed first dependency-ordered wave through PR #73 and identifies the next frontier as the Billing/Payment boundary gap audit. The current `main` also contains the subsequent Alembic-head reconciliation in PR #77.

Therefore documentation must use the following distinction consistently:

```text
Current main / implementation state  -> V1.4 ACTIVE EXECUTION BASELINE
Certified vendor release              -> v1.2.0
```

## Correction

The README was corrected to remove the misleading presentation of `v1.2.0` as the current implementation position. Release identity remains `v1.2.0` until a new vendor release is explicitly certified.

## Boundary

This correction does not create a new product release, does not change the frozen V1.4 architecture, and does not claim external production certification.
