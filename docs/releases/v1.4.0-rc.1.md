# AI Employee v1.4.0-rc.1

**Status:** POST-MERGE CERTIFICATION IN PROGRESS — NOT CERTIFIED
**Previous certified release:** `v1.3.8` at `fd1e74b6b4c1701f7443efc202bad161ff19618c`
**Candidate merge SHA:** `3f10e443a4039348a1664a0e9d851f441f94018f`
**Current certification commit:** this commit; all final evidence must bind to its exact SHA.

## Version decision

The breaking-change audit did not establish a confirmed backward-incompatible public API or contract change relative to `v1.3.8`. The target therefore remains `v1.4.0`, not `v2.0.0`.

## Certification boundary

The original candidate HEAD `7fc4a5dfa571494e4fc64590a1e694ee76f19bc8` passed the complete candidate gate topology before merge. That evidence remains historical evidence for the candidate only and is not being transferred to the post-merge identity.

The post-merge release identity is being certified independently. The current certification commit must pass the final release gate topology on its exact SHA before any stable `v1.4.0` tag is created.

Required final gates include CI, CodeQL, Production Infrastructure Validation, HA Failure Recovery Validation, Ephemeral DAST Validation, release/package integrity and migration validation, plus any additional release-specific gates required by repository topology.

`v1.3.8` remains immutable. Production deployment or external customer acceptance is a separate boundary and is not implied by this certification.
