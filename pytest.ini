[pytest]
asyncio_mode = auto
testpaths =
    backend/tests
markers =
    requires_ocr: test requires the OCR runtime available in the production-like API container
filterwarnings =
    ignore::pytest.PytestCollectionWarning
